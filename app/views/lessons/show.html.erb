<h1><%= @lesson.name %></h1>

<p><%= @lesson.body %>

<hr>

<%= link_to 'Home', courses_path %>
<%= raw "| #{link_to 'Back', course_lesson_path(@lesson.course, @lesson.prev)}" if @lesson.prev %>
<%= raw "| #{link_to 'Next', course_lesson_path(@lesson.course, @lesson.next)}" if @lesson.next %>
